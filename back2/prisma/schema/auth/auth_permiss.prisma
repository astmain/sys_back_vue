model auth_permiss {
    id     Int                    @id @default(autoincrement())
    action String                 @unique // 如 "create:post", "delete:user"
    remark String?
    roles  role_on_permiss[]
}
