// 模型商品表
model shop_order {
    order_id String @id @default(cuid())
    user_id    String // 用户id
    author_id  String // 商家id
    total_price Decimal @default(0) // 总价
    status     String @default("pending") // 状态
    created_at DateTime @default(now()) // 创建时间
    updated_at DateTime @default(now()) // 更新时间
}
